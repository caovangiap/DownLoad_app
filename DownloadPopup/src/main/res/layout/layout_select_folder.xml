<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <TextView
        android:id="@+id/tv_watch_ads"
        style="@style/TextPlanDownload"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="30dp"
        android:layout_marginBottom="20dp"
        android:layout_marginLeft="20dp"
        android:layout_marginRight="20dp"
        android:text= "@string/mes_select_folder"
        android:textSize="13sp"/>

    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/tv_watch_ads"
        android:layout_marginBottom="30dp"
        android:orientation="horizontal"
        android:layout_centerHorizontal="true">
        <Button
            android:id="@+id/btn_default_folder"
            style="@style/PlanToDownloadButton"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:background="@drawable/button_bg"

            android:paddingStart="10dp"
            android:paddingEnd="10dp"
            android:text="@string/select_default_folder"
            android:layout_marginEnd="20dp"/>

        <Button
            android:id="@+id/btn_select_folder"
            style="@style/PlanToDownloadButton"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:background="@drawable/button_bg_gray"
            android:drawableStart="@drawable/ic_folder_open_24dp"
            android:paddingStart="5dp"
            android:drawablePadding="5dp"
            android:paddingEnd="10dp"
            android:text="@string/select_folder" />


    </LinearLayout>

</RelativeLayout>